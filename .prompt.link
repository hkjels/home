#!/usr/bin/env bash

# Jobs
PS1="\[$YELLOW\]\$([ \j -gt 0 ] && echo '[\j] ') \[$RESET\]"

# Path
PS1+="\[$CYAN\]\w \[$RESET\]"

# VC
PS1+="\$(vcprompt -f \"\[$CYAN\](%n@%b) \[$YELLOW\]%u\[$RED\]%m\[$RESET\]\")"

# Prompt character λ
PS1+="\n \[$BOLD\]\[$RED\]λ \[$RESET\]"
